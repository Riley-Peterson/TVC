# Drivers that are autogenerated based on your STM32 configuration
# ignored b/c they will clog up the stm32-drivers repo when submoduled into many different stm32 repositories
# If you make an essential change, add ONLY the file with the change to version control
CMSIS
STM32*_HAL_Driver

